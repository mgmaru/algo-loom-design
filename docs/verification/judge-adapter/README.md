# JudgeAdapter技術検証の実施手順と成果物

> 対象: [`JudgeAdapter`技術検証計画](../../project/judge-adapter-verification.md)を安全かつ再現可能に実施するための準備と運用
>
> 状態: `V-01`〜`V-11`はすべて合格し、2026年8月24日に成果物確定と後始末を完了。追加P0 `V-12`は、ローカル検証物とCWS要件・承認gateを2026年8月26日に準備し、publisher登録、対象ZIPのupload、固定ID取得、Store listing・Privacy保存まで完了した。Distribution、審査、限定公開、署名済み配布物取得と、`V-12A`〜`V-12E`の同一campaignによる検証は未実施
>
> 作成日: 2026年8月11日
>
> 正本: 検証項目、優先度、合格条件は[`JudgeAdapter`技術検証計画](../../project/judge-adapter-verification.md)を優先する
>
> 認証境界: [AtCoder認証設計](../../architecture/atcoder-authentication.md)

## ドキュメント概要

本書は、検証の入力を固定する方法、一時データと成果物の分離、外部作用前の停止条件、観測方法、成果物の確定手順を定義します。検証の合格条件やMVPの範囲は変更しません。

## 0. 結論

検証で扱うデータとコードを次の4領域へ分離します。

| 領域 | 置き場所 | 保持 | 内容 |
|---|---|---|---|
| 匿名化済み成果物 | [`results/`](results/) | Gitで保持 | 観測事実、合否、リクエスト集計、初期値候補、設計への反映先 |
| 再利用可能な検証支援コード | [`scripts/verification/`](../../../scripts/verification/) | Gitで保持 | 秘密情報と実行固有値を含まず、通信と外部作用の境界をレビューできるスクリプトとローカルテスト |
| 検証用作業データ | リポジトリ外の一時ディレクトリ | 検証後に破棄 | 仮想環境、実行固有の検証コード、入出力例、提出用ソースコード、一時出力 |
| 認証情報 | 方式Cではリポジトリ外の検証用一時領域、方式Aでは検証用secret store | 成果物へ移さず検証後に破棄 | Cookie、セッショントークン。パスワードは受け取らない |

`results/`へ生のコマンド出力を複製しません。必要な値だけを許可リスト方式で転記し、[記録テンプレート](results/run-record-template.md)の機密情報検査を通してから成果物を確定します。

## 1. 準備タスク

| ID | タスク | 依存 | 完了条件 |
|---|---|---|---|
| `T-01` | 検証入力を固定する | なし | 終了済み問題、1言語、1OS、期待するアカウント、提出用ソースコードを決める |
| `T-02` | 当日の外部条件を確認する | `T-01` | AtCoderの一般利用規約、対象コンテストの個別規則、Bot対策の状態を公式情報で確認する |
| `T-03` | 隔離した実行環境を作る | `T-01` | リポジトリ外に仮想環境、作業ディレクトリ、方式Cの一時Cookie領域を用意する |
| `T-04` | 観測方法を確認する | `T-03` | 本文・秘密情報を保存せず、時刻、所要時間、状態、欠損、リクエスト数を記録できる |
| `T-05` | 方式Cで読み取りだけの検証を行う | `T-02`〜`T-04` | `V-02`、`V-01`、`V-05`の順で確認し、提出前停止条件をすべて満たす |
| `T-06` | 1件の提出を承認する | `T-05` | 問題、アカウント、言語、ソースコードのハッシュ、外部作用を人が確認する |
| `T-07` | 提出と判定を確認する | `T-06` | `V-03`、`V-04`、`V-06`、`V-08`を同じ提出IDで確認する |
| `T-08` | 安全な障害確認を行う | `T-07` | `V-07`、`V-09`を確認し、未観測の障害を意図的にAtCoderへ起こさない |
| `T-09` | 方式Aの認証を検証する | `T-02`〜`T-04` | 主要導線とは別の実行で`V-10`、可能な範囲の`V-11`を追加提出なしで確認する |
| `T-10` | 成果物を確定する | `T-08`または`T-09` | 匿名化、合否判定、初期値候補、問い合わせ用事実、正本文書への反映候補を記録する |
| `T-11` | `V-12`の製品相当検証物を準備する | `T-02`、`T-04` | 署名済み限定公開拡張機能、実行時compile不要のhelper、protocol、profile操作、campaign manifestを固定し、外部接続前の`V-12A`とtemplate不要の`V-12C`事前caseが合格する |
| `T-12` | `V-12` campaignを実行して成果物を確定する | `T-11` | 同じcampaign manifestで`V-12A`〜`V-12E`を規定順に実行し、追加提出0件、段階別の後始末、5つを集約した合否を匿名化して記録する |

`T-06`まではAtCoderへの提出を行いません。`T-06`で一つでも確認できない項目がある場合は停止し、提出回数を消費しません。`T-09`は主要導線と別の実行にでき、追加提出を行いません。`T-11`と`T-12`は工程4の追加検証であり、製品実装または正式配布の開始とは扱いません。publisher登録、支払い、審査提出または限定公開等の外部状態を変更する操作は、別途人が明示承認した範囲だけで行います。

## 2. 検証入力

実行前に次を決め、成果物の「実行条件」へ記録します。

| 入力 | 選定条件 | 成果物へ残す内容 |
|---|---|---|
| 対象問題 | 終了済みの通常のAlgorithm過去問1件。対話的問題、特殊コンテスト、個別規則が不明な問題を避ける | 正規問題ID、公式URL、終了済みと確認した時刻 |
| 言語 | 正規言語IDを一つだけ選ぶ | 正規言語ID、提出直前に解決したAtCoder固有のID・表示名・処理系・バージョン |
| OS | 実機一つだけ | OS名、バージョン、CPUアーキテクチャ。ホスト名と利用者名は残さない |
| アカウント | 検証者自身が所有する一つのアカウント | 識別情報を取得できた事実と期待値との一致。不必要な場合は実際のユーザー名を残さない |
| ソースコード | 検証者自身が作成し、対象問題へ提出してよいもの | `source-A`等の実行内別名とバイト数。本文、ハッシュ、絶対パス、生出力は残さない |

提出IDは`V-04`と`V-06`が終わるまで一時領域で保持します。成果物では`submission-A`等の実行内別名へ置き換え、公開情報との恒久的な紐付けが必要だと別途判断した場合だけ実際のIDを残します。

## 3. 当日の外部条件

実行日の直前に、少なくとも次を再確認します。

- [AtCoder利用規約](https://atcoder.jp/tos?lang=ja)
- 対象コンテストの公式ルールと終了状態
- [AtCoderのAI学習拒否設定の説明](https://info.atcoder.jp/overview/about/ai-training-opt-out)
- [`online-judge-tools`の公式リポジトリ](https://github.com/online-judge-tools/oj)と[Cloudflare関連Issue](https://github.com/online-judge-tools/oj/issues/934)
- 使用するパッケージの公開版、取得元、ハッシュまたはGitリビジョン

確認したURL、確認時刻、確認結果だけを記録します。規約本文、問題文、Issue本文の複製は成果物へ含めません。

次の場合は検証を開始しません。

- 対象問題が終了済みか判断できない
- 個別規則と一般利用規約の関係を判断できない
- ログインにCAPTCHA、Turnstileその他のBot対策の回避が必要になる
- 他人のCookie、方式C・方式Aで許可していないbrowser Cookieの取得、プロキシ切り替え等が必要になる
- 1問だけにアクセスする構成、有限の待機、リクエスト数の計測を保証できない

## 4. 隔離した実行環境

### 4.1. 作業場所

検証用作業データは、`mktemp -d`等で作ったリポジトリ外の一時ディレクトリへ置きます。シェル変数には`ALGOLOOM_VERIFY_ROOT`等のタスク固有名を使い、`HOME`を上書きしません。

一時ディレクトリには所有者だけがアクセスできる権限を設定し、次を同じ配下へ分けます。

```text
<ALGOLOOM_VERIFY_ROOT>/
├── venv/          # 検証専用のPython仮想環境
├── work/          # 入出力例、提出用ソースコード、使い捨て検証コード
├── state/         # 専用Cookieファイル等の外部ツール状態
└── scratch/       # 成果物へ転記する前の一時出力
```

リポジトリ直下の`.verification-work/`も誤操作時の退避先としてGitの対象外にしています。ただし、認証情報は除外設定だけに頼らず、原則としてリポジトリ外へ置きます。

### 4.2. 方式Cの一時session取り込み

方式Cは、検証者本人が通常のbrowserでloginして得た`REVEL_SESSION`一つだけを、入力を表示しない対話promptから検証用clientへ一度だけ取り込みます。

- Cookie値をcommand line、`argv`、environment、shell history、clipboardを読む自動処理へ渡しません。
- browserのDeveloper Toolsから値をcopyするため、clipboardへ残り得ることを実行前に確認します。検証clientがclipboardの消去を保証したとは扱いません。
- Cookie名と送信先を固定し、他Cookie、他domain、他人のsessionを受け付けません。
- 一時directoryは作成時から`0700`、一時fileは作成時から`0600`相当にし、既存fileを追記または上書きしません。
- Cookie jarへ期限を追加せず、browserまたはserverが示した属性を観測値として扱います。
- 取り込み直後に`V-02`を実行し、account identityを確認できない場合はCookieを破棄して停止します。

方式Cは今回の技術検証だけの例外であり、製品用の認証fallback、CIまたは非対話実行として残しません。

#### macOS・Google ChromeでのV-04自己検証

V-04を自己検証する場合は、リポジトリルートの**ターミナル**で次を実行します。ブラウザのConsoleで実行するコマンドやJavaScriptはありません。

```console
python3 scripts/verification/atcoder_v04_verdict.py \
  --discover-state \
  --guided-chrome
```

`--guided-chrome`はGoogle Chromeで`https://atcoder.jp/settings`を明示的に開き、Safariや既定ブラウザへfallbackしません。V-03の空の専用Chromeプロファイルは終了時に削除済みのため、この方式Cの確認では通常のChromeプロファイルを使います。ログイン画面なら本人が通常ログインと必要なTurnstileを完了して`/settings`へ戻り、既に設定画面なら再ログインせず表示アカウントを確認します。別アカウントの場合は先へ進みません。

同じChromeで`⌥⌘I`を押し、`Application > Storage > Cookies > https://atcoder.jp`から、`Name=REVEL_SESSION`、`Domain=atcoder.jp`、`Path=/`の一意な行の`Value`セルだけを非表示ダイアログへ貼り付けます。スクリプトはChromeのCookie DBやクリップボードを自動読取せず、通常のChromeを終了・削除しません。状態ファイルの選択、結果の自動保存、0件・複数件時の停止を含む詳細は[検証支援スクリプトの説明](../../../scripts/verification/README.md#atcoder_v04_verdictpy)を参照します。

#### V-03直後からV-04を観測する統合再検証

既存の最終判定済み提出から判定待ちは復元できません。V-04を再検証する場合は、方式Cの本人照合を提出前に済ませ、V-03で提出IDを取得した直後からV-04を開始する[`atcoder_v04_integrated.py`](../../../scripts/verification/atcoder_v04_integrated.py)を使用します。

この統合スクリプトは新しい提出を1件行います。[`p0-21`](results/2026-08-12-p0-21.md)で提出上限を明示承認済み実サービス検証実行ごとに最大1件へ変更し、V-03→V-04統合再検証の新規提出最大1件を許可しました。その許可は[`p0-22`](results/2026-08-12-p0-22.md)の実サービス実行で消費済みです。新たな明示承認なしに再実行しません。許可は日単位で自動更新されず、`SEND_STARTED`相当以降は成功・失敗・応答不明のいずれでも消費し、同じ許可で再提出しません。コマンド、3フェーズの手動操作、安全境界は[統合スクリプトの説明](../../../scripts/verification/README.md#atcoder_v04_integratedpy)を正とします。

統合実行では、通常ChromeでV-04方式Cセッションを準備した後、別の空の専用ChromeでV-03を人が操作します。V-03一時状態の作成をローカルで検出すると、V-03プロセスの後始末完了を待たず、同じ提出ID1件のV-04有限ポーリングを開始します。Cookie取得、本人照合、拡張読込、ログイン、エディタ往復、Turnstile、承認句、最後の提出ボタンを番号付きで案内し、提出ID取得後のV-04には追加の手動操作を挟みません。

### 4.3. ツールの固定

システムのPython環境へ直接インストールせず、検証専用の仮想環境を使います。実行時には次を記録します。

- Python、`uv`、`online-judge-tools`、`online-judge-api-client`と直接利用するHTTPライブラリのバージョン
- PyPI版なら配布ファイルのバージョンとハッシュ、Git版ならリビジョン
- パッケージ解決結果
- 実行した入口が`oj`、`oj-api`、Python API、使い捨ての薄いラッパーのどれか

`online-judge-tools`のCLIと`online-judge-api-client`の構造化出力は別々に評価します。CLI全体が動作しても、提出ID、アカウント識別情報、判定観測を安定した型で取得できなければ、`JudgeAdapter`の候補実装として無条件に採用しません。

起動時の自動更新確認等、AtCoder以外への通信もリクエスト集計と混同しないよう、送信先別に記録します。

提出一覧を反復する下位APIを使う場合、ページを一つに明示的に制限し、対象の提出IDを見つけた時点で停止します。既定の反復処理へ無制限のページ列を渡さず、過去の提出全体を取得しません。提出IDを指定して一件だけ取得する経路を実証できるかも、`V-04`と`V-06`の採否根拠へ含めます。

### 4.4. 再利用可能な検証支援コード

再検証と将来の実装判断に役立つコードは、次をすべて満たす場合に[`scripts/verification/`](../../../scripts/verification/)へ保持できます。

- Cookie、トークン、パスワード、実際のアカウント名、提出用ソースコード、入出力例の実値、生の応答を含まない。
- 認証情報を引数、環境変数、設定ファイルへ置かず、必要な場合は表示しない対話入力で受け取る。
- 送信先、HTTPメソッド、リクエスト数、間隔、タイムアウト、リダイレクト、再試行、外部作用をコードレビューで確認できる。
- ローカルテストが実サービスへ接続せず、入力検査と分類を固定入力で確認できる。
- 実行時の仮想環境、Cookie、一時JSON、生ログをリポジトリ外へ置き、成果物確定後に破棄できる。
- 製品コード、通常の認証導線、CI用の認証手段、実サービスでの合格証拠として扱わない。

保持する版が、過去に削除した使い捨てファイルを実行記録から再構成したものである場合は、バイト単位で同一と表示しません。観測事実は引き続き`results/`の実行記録を正とします。

## 5. 観測と保存の境界

### 5.1. 記録するもの

- UTCの取得時刻と単調増加時計で測った処理時間
- 操作の入口、終了コード、成功・失敗・状態不明の分類
- 許可リストへ正規化したフィールド名、型、単位、欠損
- HTTPメソッド、送信先の分類、応答状態の分類、リクエスト数、待機時間
- 入出力例の件数と入力・期待出力の対応関係を確認できた事実
- 言語候補数と、一意に解決したID、表示名、処理系、バージョン
- 判定待ちと最終判定の時刻付き観測
- `V-12`のcampaign ID、秘密でないmanifestとそのhash、環境entry、sub検証・case ID、検証層、状態遷移、合否、停止理由、外部通信集計、cleanup結果

### 5.2. 記録しないもの

- Cookie、パスワード、セッショントークン、CSRFトークン、認証用URL
- `Authorization`、`Cookie`、`Set-Cookie`を含む生のHTTPヘッダー
- 問題文、生のHTML、完全なJSON応答、入出力例の実値
- 提出用ソースコード、標準入力、標準出力、標準エラー出力
- 実際のアカウント名、メールアドレス、ホスト名、利用者名、不要な絶対パス
- 外部ツールの生ログ、例外の完全なスタックトレース
- `V-12`の一回限りの秘密値、publisher credential、署名用秘密値、実account名、profileの絶対pathと、Cookie・履歴・入力情報を含むtemplateまたはprofileの内容

`oj-api get-problem --full`のように必要な言語一覧と保存禁止の生HTMLを同じ応答へ含める入口を使う場合、応答全体をファイルへ保存しません。処理中に許可したフィールドだけへ射影し、射影後の値だけを観測記録へ転記します。

## 6. 実行ゲート

### 6.1. 読み取りゲート

次の順に実行し、各操作の間隔とリクエスト数を記録します。

1. 方式Cで本人の`REVEL_SESSION`だけを一時的に取り込み、提出せずに認証状態とアカウント識別情報を確認する（`V-02`）。
2. 同じ対象問題から公開入出力例だけを取得し、件数と対応関係を確認する（`V-01`）。
3. 対象コンテストで有効な言語一覧から、正規言語IDを一意に解決する（`V-05`）。

次のいずれかに該当したら提出へ進みません。

- アカウント識別情報を取得できない、または期待したアカウントと異なる
- ページ構造の変化と未認証を区別できない
- 入出力例の件数または対応関係を維持できない
- 言語候補が0件または複数件になる
- Adapterの通信でchallenge、429応答または外部サービスの拒否を観測した。通常browserで利用者がTurnstileを手動操作した事実だけでは停止しない
- 提出後に提出IDを構造化して取り出す経路が事前の静的確認で見つからない

`V-02`は次をすべて満たした場合だけ合格とします。

- 空のsessionによる認証必須pageへのrequestが、login pageへの許可されたredirectとして未認証に分類される。
- 方式Cのsessionによる同じrequestが成功し、許可list化した構造からaccount identityを一意に取得できる。
- 取得したidentityが、事前に固定した期待値と完全一致する。
- 200応答でもidentityを解釈できない状態を、認証済みではなくpage構造変更として区別する。
- Cookie値、生header、生HTMLを成果物または通常logへ残していない。

### 6.2. 提出ゲート

AtCoderへ送信する直前に、実行者が次を一画面で確認します。

- 正規問題IDと公式URL
- 取得済みアカウント識別情報が期待値と一致したこと
- 正規言語ID、AtCoder固有の言語ID、表示名、処理系、バージョン
- ソースコードのバイト数とハッシュ。成果物には値を残さない
- この操作が、目的と対象を事前記録して明示承認された実サービス検証実行の最大1件であること
- AtCoderのAI学習拒否設定を必要に応じて確認する公式リンク
- 送信開始後に応答が不明でも自動再送しないこと

この確認に対する明示的な承認後だけ`V-03`を実行します。確認を省略する非対話オプションや、提出前待機を無効化するオプションは使用しません。`SEND_STARTED`相当以降は結果が不明でも提出枠を消費し、同じ承認で再提出しません。日付の変更は新しい提出許可になりません。

### 6.3. 提出後

1. 応答から提出IDを取得し、`submission-A`として一時的に関連付けます（`V-03`）。
2. 同じIDだけを使い、判定待ちと最終判定を取得時刻付きで観測します（`V-04`）。
3. 最初のプロセスを終了し、別プロセスから同じIDを再照合します。再提出を呼ぶ経路は使いません（`V-06`）。
4. 返された場合だけジャッジ実行時間とメモリを単位付きで記録し、欠損時は欠損のままにします（`V-08`）。

ポーリングの上限へ達しても提出失敗とは判定しません。提出IDを保持したまま`VERDICT_PENDING`相当として終了し、後から同じIDで再確認します。

### 6.4. 方式Aの認証gate

方式Aの`V-10`は、方式Cによる主要導線と別の実行記録にします。追加提出は行わず、次を順に確認します。

1. 既存browser profileとは異なる、空の専用profileのpathを作成する。
2. Cloudflare保護ページをリモート制御状態にせず、可視browserを起動する。CDPのpipeまたはportをログイン中に使用しない。
3. 検証者がusername、password、Turnstileをbrowser上で手動操作する。
4. login完了後、`p0-23`で安全性とCloudflare互換性を確認した検証境界から、helperが`https://atcoder.jp`の`REVEL_SESSION`だけを取得する。CDPへ自動的に戻さない。
5. 同じsessionで`V-02`と同じaccount identity確認を行う。
6. 期待値一致後だけ検証用secret storeへ保存し、processを再起動して同じidentityを確認する。
7. browserを終了し、一時profileと制御channelを後始末する。
8. Cookie、password、生header、既存profileのdataがlog、scratch、成果物へないことを検査する。

次のいずれかでは`V-10`を不合格または中止とし、回避策を追加しません。

- 可視の専用browserでも通常のloginを完了できない。
- headless化、fingerprint偽装、stealth設定、Turnstile自動操作が必要になる。
- 必要なCookieだけへ限定できない、または既存profileを参照する必要がある。
- account identityを一意に確認できない、または期待値と一致しない。
- debug channelをlocal child processへ限定できない。
- Cloudflare保護ページで`navigator.webdriver`が真になる、または公式互換性チェッカーのAutomation Checkに失敗する。
- Cookie値を成果物へ残さずに合否を説明できない。
- 異常終了時のbrowser processまたは一時profileを安全に回収できない。

`V-11`では通常操作中の`Set-Cookie`だけを観測し、session維持目的のbackground requestを送信しません。serverが返さないexpiryを補わず、更新が観測されなかった場合もその事実を記録します。

#### V-11のCookie更新・失効検証

V-11は、実サービスの通常観測と、AtCoderへ障害を起こさないローカル制御を分けます。

実サービスではV-10と同じ方式Aの境界を使い、人がログインと必要なTurnstileを操作した後、`REVEL_SESSION`だけを取り込みます。ヘルパーは本人確認に必要な`GET /settings`だけを最大3回、各開始の間隔を2秒以上として実行します。POST、提出、自動再試行、リダイレクト追従、セッション維持だけを目的とする通信は行いません。

応答の`Set-Cookie`は、ヘッダーの有無と件数、`REVEL_SESSION`指示件数、値の変更有無、明示期限属性の有無と由来へ射影します。生ヘッダー、Cookie値、実際の期限時刻は保存しません。値が変わった場合は候補値で同じ本人アカウントを確認した後だけ、世代一致を条件とするKeychain更新で永続化します。プロセス再起動相当の新しいプロセスが更新後のレコードを読み、同じ本人を確認できた場合だけ成立とします。

次は合成値と一時Keychain項目によるローカル制御で確認します。

- `Set-Cookie`なし、同値、更新値、`Expires`、`Max-Age`、空値または期限経過による明示失効
- サーバーが期限を返さない場合に期限不明のままとすること
- サーバー由来の明示期限を過ぎた場合に外部通信前で停止すること
- 世代付きの原子的な置換、古い世代による更新の拒否、勝者値の保持
- 秘密情報保管庫の読取・書込障害と更新競合を、提出前停止へ関連付けること
- 平文ファイルへのフォールバックを持たず、一時Keychain項目を後始末すること

実サービスで期限切れや競合を意図的に発生させません。通常操作中に明示失効またはログイン誘導が返った場合は、その実測を記録して安全側で停止します。返らなかった場合は、実サービス未観測とローカル制御を区別したまま合否を判定します。

### 6.5. `V-12` 方式A製品形態の実行gate

製品相当検証物のsource、build、manifest、profile操作、ローカル事前testは[`scripts/verification/atcoder_v12/`](../../../scripts/verification/atcoder_v12/)を使います。CWSの最新要件、listing・privacy原稿、登録・支払い・upload・審査・限定公開・停止の承認境界は[`V-12 Chrome Web Store配布準備`](v12-chrome-web-store-preparation.md)を正とします。外部承認前はCWSの状態を変更せず、ローカルtestの成功を署名済み限定公開版の準備完了へ読み替えません。

`V-12`の合格条件と結果無効化規則は[検証計画 §3.1.1](../../project/judge-adapter-verification.md#311-v-12-方式a製品形態の検証)を正とします。本節は、同じ検証物と状態を安全に受け渡し、外部通信と後始末を実行時に制限する手順を定義します。

#### campaign開始gate

次をすべて確認するまでcampaignを開始しません。

1. リポジトリ外に所有者だけがアクセスできるcampaign用一時directoryを作り、build、実行状態、scratch、caseごとのprofileとsecret namespaceを分離する。
2. campaign IDとmanifestを一つ確定する。manifestには検証計画revision、同意版、拡張機能の固定ID・対象版・更新test用の版の組・配布元・権限・source revision・build hash、helper版・hash、protocol版、Chrome版、OS・CPU architecture、template schemaを含める。秘密値と実account名は含めない。
3. 署名済み拡張機能が審査済み・限定公開中であり、公開範囲、owner、停止方法を確認できる。publisher credentialと署名用秘密値をrepository、manifest、通常logへ置かない。
4. 使用するChrome、OS、秘密情報保管庫、配布物と当日の外部条件を固定し、manifestの環境entryと一致させる。
5. `V-12A`を外部通信0件で実行でき、caseごとのprocess、待受、file lock、一時profile、未確認sessionの残存を開始前後に検査できる。
6. 初回導線を開始する前に、対象account、取消操作、各sub検証の通信上限、追加提出0件、campaign終了時の削除対象を人が確認する。

manifestは各sub検証の開始時に再照合します。製品挙動へ影響する不一致を見つけた場合は値をその場で更新せず、現在のcampaignを停止します。検証計画の無効化表で影響する結果を判定し、必要なら新しいcampaign IDを作ります。

#### sub検証ごとの通信上限

| sub検証 | 許可する外部通信 | 上限 | 禁止する通信・作用 |
|---|---|---|---|
| `V-12A` | なし | 外部通信0件 | Chrome Web Store、AtCoder、Cloudflareその他への接続 |
| `V-12B` | 固定したChrome Web Storeの対象拡張機能ページと、通常Chromeが標準追加に必要とするGoogle管理のresource | Store pageを開く操作1回、対象版の標準追加1回。利用者またはCLIが開始するnavigationと追加操作を記録し、browser内部resourceのtransport件数は固定しない | AtCoderへの遷移、Google account追加・同期、developer mode、policy、外部拡張設定、OS registryの変更 |
| `V-12C` | 契約test・3 OS統合testはなし。配布導線caseだけ、固定した対象拡張機能の標準追加または更新に必要な通信 | 外部通信0件を原則とし、配布導線caseはcaseごとに追加または更新を最大1操作。送信先別に集計する | AtCoder・Cloudflareへの接続、通常系Chrome環境や実サービスsessionへの故障注入 |
| `V-12D` | 通常ChromeによるAtCoder loginとCloudflareの通常通信、helperによる`GET /settings` | `GET /settings`最大2回。人が行うlogin formの通信は許可し、browserのnavigationと手動操作を記録する。helper・CLIからAtCoderへのPOST、提出formのPOST、提出、自動再試行、session維持目的の通信は0件 | 提出pageへの遷移、提出formのPOST、Bot対策の自動操作・回避、直接HTTP提出 |
| `V-12E` | 実サービスsmokeでは通常ChromeによるAtCoder login、Cloudflareの通常通信、対象の提出確認pageへの遷移、helperによる`GET /settings`。3 OS統合testはstubだけ | `GET /settings`最大1回。人が行うlogin formの通信は許可し、browserのnavigationと手動操作を記録する。helper・CLIからAtCoderへのPOST、提出formのPOST、最後の提出操作、追加提出、自動再試行は0件 | AtCoder側sessionの意図的失効、直接HTTP提出、実サービスを使う故障注入 |

ChromeやChrome Web Storeが内部で行うresource requestはtransport単位の固定件数を前提にできないため、利用者またはCLIが開始するnavigation・追加・更新操作を上限にします。件数確認だけのためにCDP、proxy、`webRequest`権限または生のnetwork logを追加しません。固定した操作以外のbrowser遷移、challenge、429、helper・CLIの禁止されたAtCoder向けrequest、提出formの送信eventまたは拒否を観測したら停止します。

#### 実行順と分断禁止

次の順で実行し、前段が合格しない限り後段へ進みません。

1. `V-12A`を独立実行する。不合格ならChrome Web StoreとAtCoderへ接続しない。
2. `aloom auth login`相当から`V-12B → V-12D`を一連の初回導線として実行する。初回同意、標準追加、自動検出、Chrome完全終了、基準template確定、実行用profile起動、AtCoder login、Cookie受領、本人確認、secret store保存、新process再照合、CLI結果表示までを続ける。
3. `V-12D`の実行用profile、未確認session、待受処理を回収する。確認済みlocal sessionは隔離し、AtCoderのCookie、履歴、入力情報を持たない基準templateだけを`V-12E`まで保持する。
4. `V-12C`をcaseごとに独立実行する。template不要caseは新しい使い捨てprofile、template必要caseは基準templateの使い捨て複製、環境更新caseは隔離したsnapshotを使う。caseごとにsecret namespaceを分ける。
5. `V-12E`の直前に、AtCoder側sessionを失効させず、製品の削除契約に従って確認済みlocal sessionだけを除去する。別のCLI実行の`submit`相当から、再認証理由・中止方法表示、browser自動起動、AtCoder login、同じbrowserの提出確認画面までを一連で確認する。

`V-12B → V-12D`、`V-12D`内の「login → Cookie受領 → 本人確認 → 保存 → 新process再照合」、`V-12E`内の「submit開始 → 再認証 → 同じbrowserで提出確認」は、観測の転記、CLIへの入力、利用者によるbrowser・helperの再起動または配布物の再buildで分断しません。`V-12B`で必要なChrome完全終了と、基準templateの一時複製を使うbrowserの自動再起動は、同じCLI実行内の規定手順として続けます。利用者にURL、code、Cookieのcopy-and-paste、初期化pageへ戻る操作、拡張機能画面の操作または手動のpage探索を求めません。

取消、timeout、browser終了、helper異常終了等は実行を終了させるため、正常導線へ注入せず`V-12C`の独立caseにします。各caseは開始前の状態、注入点、期待する安全側停止、cleanup結果を固定し、別caseの残存状態を入力にしません。

#### 後続へ進まない停止条件

次のいずれかを観測したら、該当sub検証を不合格または中止とし、依存する後続へ進みません。

- 審査済み・限定公開状態、固定ID、版、配布元、権限、source revision、build hashまたはmanifest一致を確認できない。
- 標準追加にdeveloper mode、ChromeへのGoogle account追加・同期、企業向けpolicy、外部拡張設定、OS registryまたは既存profileの変更が必要になる。
- 権限が最小範囲を超える、折返し通信の`Host`・送信元・本文上限・状態順序・一回限りの秘密値を検査できない、`REVEL_SESSION`以外を読み取る。
- Chromeの完全終了、templateの無秘密性・完全性、正常な複製・破棄または固定IDによる導入完了の自動検出を確認できない。
- CloudflareまたはAtCoderに拒否される、`navigator.webdriver`が真になる、CDP、WebDriver、remote debugging、headless化、stealth、指紋偽装またはTurnstile自動操作が必要になる。
- 本人accountを一意に照合できない、未確認sessionを確定保存する、secret storeへの保存・新process再照合に失敗する。
- 初回または`submit`からの再認証で、分断禁止区間、手動操作の上限、一往復または同じbrowser内の遷移を維持できない。
- 一回限りの秘密値、Cookie、publisher credential、署名用秘密値または実account名が配布物、通常log、error、成果物、Gitへ混入する。
- case終了時にprocess、待受、file lock、一時profile、未確認sessionを回収できない、または基準template、通常系Chrome環境、確認済み保存sessionを故障caseから隔離できない。
- 規定した外部通信上限を超える、helper・CLIが禁止されたAtCoder向けrequestを開始する、提出formの送信eventが発生する、最後の提出操作または追加提出が必要になる。

停止後は手動読込、CDP、WebDriver、企業向けpolicy、候補B・C、直接HTTP提出へ切り替えません。方式Aの再設計またはMVP範囲の再検討へ戻します。

#### 証拠と後始末

`V-12A`〜`V-12E`ごとに、入力の別名、manifest hash、環境entry、検証層、外部通信集計、状態遷移、手動操作、合否、停止理由、秘密情報検査、cleanup結果を記録します。`V-12C`はcase IDごとに分け、実サービス、配布導線、3 OS統合、OS非依存の証拠を混ぜません。

各case終了時に、実行用profile、未確認session、一回限りの秘密値、待受処理、子process、file lockを回収します。campaign終了時には基準template、store用一時情報、検証用secret store項目を削除します。限定公開拡張機能を残す場合は、公開範囲、目的、owner、停止方法を成果物へ記録します。基準templateは`V-12B`で一度だけ確定し、`V-12E`まで再作成せず、終了後に削除します。

`V-12`全体は、5つすべてが同じ最終campaign manifest revisionで合格した場合だけ合格です。結果文書だけの修正や独立した故障caseの追加で無関係な実サービスsmokeを再実行しません。manifestを変更した場合、旧結果を新revisionへ直接付け替えず、検証計画の無効化規則に従います。再利用可能な結果は、入力projectionのhash一致、旧campaign IDと証拠を記した新しいsub結果として再承認し、影響を受ける結果だけを再実行します。

## 7. 障害確認

障害の証拠を次の3段階に分けます。

| 証拠 | 意味 | 例 |
|---|---|---|
| 実サービスで観測 | 通常操作中にAtCoderから実際に得た事実 | 未認証、判定待ち、最終判定 |
| ローカルで制御 | AtCoderへ負荷を掛けず、使い捨て検証コードで起こした事実 | 接続失敗、応答遅延、待機指示、形式不正 |
| 静的確認 | 使用ライブラリの公開インターフェースや実装を確認した事実 | 接続とリクエストの上限を別々に指定できるか |

`V-07`と`V-09`のために、AtCoder側の拒否、レート制限、ページ変更、通信障害を意図的に発生させません。空の専用Cookieファイルによる未認証確認と、ローカルの制御可能なHTTP応答を優先します。実際に観測していない状態は、別の障害を根拠に「観測済み」としません。

期限切れセッションを確認するために、使用中のブラウザセッションを複製または破壊しません。安全に用意できない場合は未観測とし、どの例外や応答を分類境界にする予定かを静的確認として分けて記録します。

## 8. 成果物の確定

1. [記録テンプレート](results/run-record-template.md)を同じディレクトリの`YYYY-MM-DD-<run-id>.md`へ複製します。
2. 各検証項目を`未実施`、`合格`、`不合格`、`未観測`、`中止`のいずれかにします。`V-12`では`V-12A`〜`V-12E`と`V-12C`の各caseを個別に記録し、最後にtop-levelの合否を集約します。
3. 実サービス、ローカル制御、静的確認の証拠を混ぜずに記録します。
4. `V-01`〜`V-04`または`V-10`が一つでも不合格ならMVP実装開始条件を不合格とします。`V-12A`〜`V-12E`が同じcampaign manifestで全合格でなければ`V-12`を未合格とし、方式Aを製品採用しません。いずれも回避実装案ではなく範囲と価値仮説の再検討先を記録します。
5. P1が不合格なら影響する機能と見直す正本文書を記録します。
6. 未決事項2.3の数値は「初期値候補」とし、決定値として書き換えません。
7. 未決事項8.1へ渡すリクエスト数、間隔、保存内容、認証方式を集計します。
8. `online-judge-tools`、薄いHTTP Adapter、方式Aの認証helperを別々に評価し、認証、account確認、提出ID、判定確認の根拠を分けます。
9. 一時データを破棄する前に成果物だけで合否の根拠を説明できるか確認します。
10. 機密情報検査後に成果物をGitへ追加します。
11. `V-12`ではmanifest変更の有無と無効化した結果、caseごとの一時資源0件、campaign終了時の基準template・store用一時情報・secret store項目の削除を確認します。

成果物の確定後、検証用作業データ、方式Cの一時Cookie、方式Aの検証用secret store項目と専用browser profileを削除します。外部ツールが利用者の通常環境へ作成した認証状態がないかも確認します。削除対象は作成時に記録した一時ディレクトリとAlgoLoom namespaceの正確な項目に限定し、広いパスや未解決の変数を削除対象にしません。localのCookie削除をAtCoder server上のsession失効とは記録しません。

## 9. 検証の最終状態

| 項目 | 状態 | 備考 |
|---|---|---|
| 成果物ディレクトリ | 完了 | `results/`を作成済み |
| 記録テンプレート | 完了 | `V-01`〜`V-11`、認証方式、リクエスト集計、判断欄を用意済み |
| 一時データの除外 | 完了 | `/.verification-work/`をGitの対象外に設定済み |
| 実行手順と停止条件 | 完了 | 本書に定義済み |
| `V-02`検証支援コード | 完了 | [`atcoder_v02_session_check.py`](../../../scripts/verification/atcoder_v02_session_check.py)と、実サービスへ接続しないローカルテストを保持。`p0-04`の削除済みファイルとバイト単位で同一とは扱わない |
| `V-03`検証支援コード | 完了 | [`atcoder_v03_browser_submit.mjs`](../../../scripts/verification/atcoder_v03_browser_submit.mjs)と検証専用拡張で通常Chrome、人による拡張読込・ログイン・エディタ往復・Turnstile・提出操作、認証付きloopbackを実装。往復後・承認時・送信時に可視プレーンテキスト欄と直列化されるソースを検査して、不一致を送信前遮断する。`p0-16`で検証済み |
| `V-04`検証支援コード | 完了 | [`atcoder_v04_integrated.py`](../../../scripts/verification/atcoder_v04_integrated.py)でV-04準備、V-03、状態作成直後のV-04を統合。`p0-22`で同じIDの判定待ち5回と最終判定`AC`を時刻付きで取得し、V-04は合格 |
| `V-06`検証支援コード | 完了 | [`atcoder_v06_recovery.py`](../../../scripts/verification/atcoder_v06_recovery.py)で、所有者専用状態を別プロセスから読み、本人照合後に対象ID 1件だけをGETする。`p1-01`で追加提出なしに`FINAL`と`AC`を再取得し、V-06は合格 |
| `V-07`検証支援コード | 完了 | [`atcoder_v07_auth_failures.py`](../../../scripts/verification/atcoder_v07_auth_failures.py)で、認証情報を受け取らず、固定入力13件と任意のCookieなしGET 1回から未認証、期限切れ、拒否、ページ構造変更、通信障害を分類する。`p1-02`で13/13一致と実サービスの未認証対照を確認し、V-07は合格 |
| `V-08`検証支援コード | 完了 | [`atcoder_v08_metrics.py`](../../../scripts/verification/atcoder_v08_metrics.py)で、本人提出一覧の固定フィルタ・先頭1ページから対象候補を一意にし、判定、実行時間、メモリをnullableな共通単位へ射影する。`p2-01`で11 msと9,172 KiBを取得して11 msと9,392,128 byteへ正規化し、欠損時の判定保存継続も確認 |
| `V-10`検証支援コード | 完了 | [`atcoder_v10_session.mjs`](../../../scripts/verification/atcoder_v10_session.mjs)、検証専用拡張、Security Frameworkヘルパーで、空の可視Chrome、Cookie限定取得、本人照合、Keychain保存、新規プロセス再照合、後始末を確認。`p0-23`で方式Aを検証済み |
| `V-11`検証支援コード | 完了 | [`atcoder_v11_cookie_lifecycle.mjs`](../../../scripts/verification/atcoder_v11_cookie_lifecycle.mjs)とSecurity Frameworkヘルパーで、Cookie更新、明示期限、Keychainの世代付き置換、新規プロセス再照合、失効・競合・保管庫障害の提出前停止を確認。`p1-03`で検証済み |
| 対象問題・言語・アカウント | 完了 | `p0-22`で`abc300_a`、期待する本人アカウントとの一致、CPython 3.13.7候補1件を再確認。実際のアカウント名は保存していない |
| AtCoderへの提出承認 | 完了・許可消費済み | `p0-22`で人によるTurnstileと明示承認後、フォーム`submit`イベント1回、結果ページ遷移、AtCoder発行の提出ID取得を確認。新規提出1件、自動再送0回、同一承認内の再提出0回 |
| `V-01`〜`V-11` | 完了 | P0は5/5、P1は4/4、P2は2/2で、全11項目が合格。MVP実装開始条件1〜3に対応する技術検証を充足。詳細は[`p0-01`](results/2026-08-11-p0-01.md)〜[`p2-01`](results/2026-08-13-p2-01.md) |
| `V-12`実行計画 | CWS item準備進行中・実行未実施 | `V-12A`〜`V-12E`を一つのP0 gateとして定義済み。2026年8月26日に[`atcoder_v12/`](../../../scripts/verification/atcoder_v12/)へ拡張source、事前build helper、protocol、profile操作、manifest検査と固定入力testを追加し、[CWS配布準備](v12-chrome-web-store-preparation.md)へ公式要件と外部承認gateを記録した。publisher登録と支払い、明示承認された`0.1.0` ZIPによる新規item作成・upload、固定IDのowner-only記録、承認済みStore listing・Privacyの保存まで完了した。Distribution、審査、限定公開、署名済み配布物取得が未完了のため`T-11`と`V-12`は未完了 |
| 一時データと認証情報の破棄 | 完了 | 2026年8月24日に、リポジトリ外の検証用一時ディレクトリ、方式Cの一時認証ファイル、方式Aの検証用秘密情報保管庫項目、専用ブラウザプロファイル、ゴミ箱内の検証用データの残存が0件であることを確認。ローカルでの削除をAtCoder側のセッション失効とは扱わない |
| リポジトリ内の一時領域 | 完了 | `.verification-work/`の残存は0件、版管理の対象外設定は有効、同ディレクトリ配下の追跡対象は0件 |
| 通常環境への認証状態 | 完了 | 外部ツールの認証保存先が検証用一時領域に限定され、専用ブラウザを使う経路が一時プロファイルを削除したことを確認。通常ブラウザに利用者が保有する認証状態は削除対象にせず、検証支援コードが通常環境へ認証情報を保存した事実はない |
| 検証支援コードの機密情報検査 | 完了 | `scripts/verification/`に実際の認証情報、アカウント名、提出ID、提出用ソースコード、実行固有の絶対パスと生成物がないことを確認。外部通信を行わない全168件の試験も合格 |
| `V-01`〜`V-11`技術検証の終了 | 完了 | 匿名化済み成果物だけで合否の根拠を説明でき、全11項目の合格、成果物の確定、後始末を確認したため、2026年8月24日に従来の検証を正式終了。追加した`V-12`のcampaignはこの完了実績と分けて管理する |

## 10. 事前調査で判明した注意点

2026年8月11日に、AtCoderへ接続せず、パッケージの公開情報、ヘルプ、インストール済みコードだけを確認しました。次は`V-01`〜`V-11`の観測結果ではなく、実行環境を用意するための注意点です。

| 観点 | 確認した事実 | 準備への反映 |
|---|---|---|
| 公開版 | PyPIの`online-judge-tools`最新版は`11.5.1`で、公開日は2021年9月13日 | バージョンだけで現在のAtCoder対応を推測せず、当日に実動を検証する |
| Python 3.12 | `uv`が既定で選んだPython 3.12.13では、`distutils.version`の読み込みに失敗してCLIが起動しなかった | Pythonのバージョンを明示し、AtCoderへ接続する前に全入口の起動確認を行う |
| Python 3.9 | macOS付属のPython 3.9.6ではCLIを起動できたが、現在解決された`urllib3`からTLS実装に関する警告が出た | 直接・間接依存を固定し、TLS警告を無視したまま提出へ進まない |
| 自動通信 | `oj --version`の実行時にもPyPIへの更新確認が発生した | AtCoder以外への通信を別集計にし、再現に不要な自動通信を識別する |
| 認証確認 | `oj login --check`はログイン済みかを返すが、アカウント識別情報は返さない | `V-02`には、同じセッションから識別情報を安全に抽出する別の検証経路が必要 |
| 提出ID | `oj-api submit-code`の構造化出力は提出URLを持ち、URLから提出IDを得られる設計になっている | `V-03`ではCLIの真偽値ではなく、下位APIの提出結果を確認する |
| 判定確認 | `oj-api`の公開サブコマンドには提出IDを指定する判定取得がなく、Python APIの提出詳細取得は非推奨かつ未実装になっている | `V-04`と`V-06`は無条件に成立するとみなさず、追加の薄い検証コードが必要 |
| 提出一覧 | Python APIの提出一覧反復は、ページ上限を渡さない場合に後続ページを走査し得る | 一ページへ制限し、対象IDを得た時点で停止する。制限できなければ実行しない |
| Bot対策 | 公式リポジトリには、AtCoderのCloudflare導入後に自動browser loginが失敗する未解決Issueがある。`p0-10`で現行CDP方式も自動化状態になることを確認し、`p0-23`でCDP等を使わない通常Chrome状態の方式Aを確認 | password型loginと現行CDP方式を再試行しない。方式Aでも人がloginとTurnstileを操作し、拒否時は回避方法を追加しない |
| 既存の`atcoder-login.sh` | `p0-01`・`p0-02`で使ったパスワード入力型経路を再現するヘルパーであり、方式Cには対応していない | 過去結果の再現用として扱う。`p0-03`と`p0-04`では、方式C専用の使い捨て検証コードをリポジトリ外で実行した |
| `atcoder_v02_session_check.py` | `p0-04`で記録した方式Cの確認手順を、Python標準ライブラリだけで再現する | 認証と`/settings`閲覧の読み取り専用検証に限定する。提出認可の確認、製品認証、CIには使用しない |

この事前調査だけでは、現在のAtCoderに対するP0の合否は判定しません。

2026年8月11日の`p0-02`では、時間を空けてパスワード入力型CLIの認証を1回再実施しました。標準の認証確認は302のログイン誘導となり、公開ログインページにはTurnstileのマーカーが残っていました。登録直後の反映遅延だったという仮説を支持する観測は得られず、原因を一意に分類できないため停止条件に従って提出せず終了しました。

同日の設計判断で、過去の観測記録を変更せず、方式Cを検証専用の限定経路として許可しました。`p0-03`では方式Cで`V-02`を実行しましたが、空のセッションと取り込んだセッションがともにログイン誘導となり、不合格としました。その後の`p0-04`では、同時点の通常ブラウザで本人アカウントとCookie対象行を確認してから再実施しました。空のセッションは302、`REVEL_SESSION`一つだけを付けた通信は200となり、識別情報が期待値と一致したため、現在の`V-02`を合格とします。`p0-03`の直接原因は当時の値が認証済みとして扱われなかったことですが、期限切れ、対象行の選択相違、転記相違は証拠上分離できません。

`p0-04`の成果物確定時には当時の使い捨て検証コードを削除しました。その後、再検証と将来の実装判断へ利用するため、記録済みの通信条件と分類をもとに[`atcoder_v02_session_check.py`](../../../scripts/verification/atcoder_v02_session_check.py)を再構成しました。この版のローカルテスト成功を`p0-04`の実サービス証拠へ遡及させず、削除済みファイルとバイト単位で同一とも扱いません。

同日の`p0-05`では、[`atcoder_v03_submit.py`](../../../scripts/verification/atcoder_v03_submit.py)で`V-03`の実行を開始しました。当日の`V-02`再確認は合格しましたが、認証済み提出ページの200応答内にTurnstile関連マーカーを観測し、安全条件に従ってフォーム解析前に停止しました。`V-05`と`V-03`は中止、GETは3回、POSTと提出は0件です。マーカーの存在だけからチャレンジ画面、通常フォーム内の部品、提出に必要な追加情報を区別せず、回避実装へ進みません。`V-03`、`V-04`、方式Aの`V-10`は未合格であり、P0全体とMVP実装開始条件は未充足です。

同日の`p0-06`では、Cloudflare公式の判定とフォーム構造分類を追加した同スクリプトで`V-03`を再実行しました。`V-02`再確認は合格し、提出ページの`cf-mitigated`がないためCloudflare Challenge Pageではないと分類できました。フォーム1件、POST方式、CSRF入力、対象問題、ソースコード欄は確認できましたが、静的HTMLの言語選択要素と`python-cpython`候補は0件で、`V-05`を不合格として停止しました。対象フォーム内のTurnstileウィジェットも1件観測しています。GETは3回、POSTと提出は0件です。`V-03`、`V-04`、方式Aの`V-10`は未合格であり、P0全体とMVP実装開始条件は未充足です。

2026年8月12日の`p0-07`では、公式JavaScript実行前の問題別・名前なし言語`select`に対応した同スクリプトで`V-03`を再実行しました。`V-02`再確認は合格し、提出ページはCloudflare Challenge Pageではないと分類できました。名前なし言語`select`を8件、対象問題用`select`を1件認識し、CPython候補を1件へ一意に解決できたため`V-05`は合格です。続いて対象フォーム内のTurnstileウィジェット1件を観測し、回避せず`V-03`をPOST前に中止しました。GETは3回、POSTと提出は0件です。`V-03`、`V-04`、方式Aの`V-10`は未合格であり、P0全体とMVP実装開始条件は未充足です。

同日の`p0-08`では、空の専用プロファイルと可視Chromeを使い、JavaScript実行後のTurnstile応答欄を提出なしで観測しようとしました。検証者がログイン時のCloudflare検証失敗を確認したため、同じ条件で再試行せず中止しました。Cookie、トークン値、HTML、ヘッダー、画面、ソースコードは取得せず、提出は0件です。実行時版はログイン前から対象提出フォーム用のガードを注入していたため、その変更がCloudflare判定へ影響しなかったとは証明できず、この実行単独では方式Aの恒久的な不成立と断定しませんでした。実行後の再利用版はログイン中のページ注入をなくし、ログイン完了後にだけ提出防止を追加する二段階へ修正しました。

同日の`p0-09`では、修正版を新しい実行として再実施しました。既存Chromeを通常終了して関連プロセス0件を確認し、ログイン中のページ注入なし、空の専用プロファイル、VPNなし、システムプロキシ無効、ネットワーク切り替えなしの条件へ固定しましたが、検証者がログイン時のCloudflare検証失敗を再び確認したため中止しました。対象提出ページへ移動せず、Cookie、トークン値、HTML、ヘッダー、画面、ソースコードは取得せず、提出は0件です。Cloudflare公式のトラブルシューティングと対応ブラウザ説明を照合し、現在の`--remote-debugging-pipe`によるブラウザ制御方式を同じ条件変更だけで反復せず、方式Aの設計判断へ戻す観測として保持します。

同日の`p0-10`では、AtCoderへ再接続せずCloudflare検証失敗を切り分けました。現行ヘルパーと同じ`--remote-debugging-pipe`、CDP接続、ターゲット接続、ページ移動をローカルの`data:` URLで再現すると`navigator.webdriver: true`でした。一方、同じ端末、回線、Chromeと別の空プロファイルを使い、CDPなしでCloudflare公式互換性チェッカーを開くと、検証者は実Turnstileを含む`Diagnostics passed`を確認しました。現行CDP方式をCloudflare非互換と判定し、再接続、検知隠蔽、回避設定、提出を行わず、通常ブラウザ状態を維持する方式Aの境界再設計へ戻します。

同日の`p0-11`では、通常Chromeと検証専用拡張を使うV-03経路を作成しました。拡張はCookie、CSRFトークン、Turnstileトークンを読み取らず、ログインページで動作せず、AtCoder本体の提出ボタンを自動操作しません。公式Chrome 137以降の`--load-extension`廃止に合わせ、空の専用プロファイルへ利用者が拡張を手動読込する方式へ修正しました。ローカルテストは成功しましたが、実行では拡張初期化完了を観測できず、CloudflareとAtCoderへ移動する前に停止しました。提出は0件で、V-03は未実施のままです。

同日の`p0-12`では、通常ChromeからCloudflare公式互換性、本人アカウント、対象フォーム、CPython 3.13.7、提出前一覧0件を確認しましたが、承認前のloopback 409で停止しました。`p0-13`は拡張初期化待ちの5分上限で停止し、外部ページへ未到達です。両実行ともフォーム`submit`イベントは0件でした。

同日の`p0-14`では、同じ通常Chrome経路で人によるTurnstileと提出ゲートの明示承認まで成立し、AtCoder本体の提出ボタン操作後にフォーム`submit`イベントを1回観測しました。結果ページへ遷移せず、利用者が画面上のソースコード空欄を報告したため再提出せず停止しました。Cookie・network監視権限を持たないためHTTP POSTの到達は断定せず、提出IDも得ていません。実行後、プレーンテキスト欄の可視性と直列化されるソースを承認時・送信時に検査し、不一致なら既定送信を同期的に遮断する修正を加えました。検証計画の1回上限を消費した可能性があるため、同じ計画では追加POSTを行いません。

同日の`p0-15`では追加提出を行わず、p0-14版がAce表示中の非表示`textarea`だけへソースを設定し、Ace内部の空状態を同期していなかったことを原因と特定しました。AtCoder本体の同期処理を通すため、人がAceからプレーン欄、Ace、プレーン欄へ切り替えてAce上のソースを目視確認する往復を必須化しました。往復後・承認時・送信時には、DOM構造、可視状態、対象問題・言語、`textarea`本文・バイト数、`FormData`の直列化値を再検査し、不一致を同期的に遮断します。ローカルではp0-14の状態と送信時の空値上書きを再現して防止を確認しましたが、実サービスへは接続していないため`V-03`の合否を変更しません。

同日の`p0-16`では、新しい1回上限の実行として通常Chrome経路を再実施しました。最初の2実行は、現行DOMにない追加CSS classと、正本文書より厳しい要素間の祖先関係を検証コードが要求していたため、承認・提出前に停止しました。条件を一意な要素、対象フォーム所属、可視モード、`FormData`一致へ修正し、各修正後にローカル72件を通しました。3実行目でCloudflare互換性、本人アカウント、CPython 3.13.7、エディタ往復、Turnstile、明示承認を確認し、人による提出1件が受理されAtCoder発行の提出IDを取得したため、`V-03`は合格です。実際の提出IDは`V-04`・`V-06`用の所有者専用一時状態にだけ保持しています。

同日の`p0-17`では、[`atcoder_v04_verdict.py`](../../../scripts/verification/atcoder_v04_verdict.py)を作成し、`p0-16`の提出ID1件だけをAtCoder公式画面と同じ判定状態経路で確認しました。初回は現行応答の複数ルート要素を単一要素へ固定した過剰条件で停止し、公式`contest.js`に合わせて対象ID付き待機要素と最終判定コードの一意性へ修正しました。再実行では本人照合と最終判定`WA`の時刻付き取得に成功しました。V-03から約28分後で判定待ちは未観測のため、`V-04`は一部観測の未合格です。2実行の合計はGET 4回、POST・追加提出・自動再試行0回です。

同日の`p0-18`では、V-04の手動操作説明が「通常ブラウザ」に留まり、既定ブラウザでSafariが開き得ること、ログイン要否とCookie取得の操作が一連の手順として明示されていなかったことを是正しました。推奨入口を`--discover-state --guided-chrome`とし、Google Chromeの明示起動、通常プロファイルを使う理由、ログイン済み・未ログイン・別アカウントの分岐、DevToolsの画面操作、非表示入力を段階ごとに表示します。修正とローカル確認だけを行いAtCoderへ接続していないため、`p0-17`の実サービス観測と`V-04`の未合格は変更しません。

同日の`p0-19`では、`p0-18`の推奨入口を実サービスで実行しました。Google Chromeの明示起動、通常プロファイルでの本人確認、Cookie対象行の手動確認、非表示入力、通信承認を完了し、本人照合と`submission-A`の最終判定`WA`を再取得しました。検証支援コードの通信はGET 2回、POST・追加提出・自動再試行0回です。既存提出は既に最終判定済みで判定待ちは未観測のため、`V-04`は一部観測の未合格を維持します。

同日の`p0-20`では、V-04方式Cセッションを提出前に準備し、V-03状態の作成直後から同じ提出ID1件のV-04有限ポーリングを開始する統合支援コードを実装しました。通常Chrome、空の専用Chrome、自動観測を3フェーズへ分け、拡張読込から提出までの人の操作を番号付きで案内します。ローカルテストだけを行い、現在の1件上限を変更していないためAtCoderへの追加提出と実サービス観測は0件です。`V-04`の一部観測は変更しません。

同日の`p0-21`では、提出上限を「検証全体で合計1件」から「目的と対象を事前記録して明示承認した実サービス検証実行ごとに最大1件」へ変更しました。日次リセットではなく、`SEND_STARTED`相当以降は結果にかかわらず枠を消費し、同じ承認で再提出しません。`abc300_a`と`python-cpython`を対象に、統合支援コードを使うV-03→V-04再検証の新規提出最大1件を許可しました。本変更ではAtCoderへの接続、ブラウザ起動、提出を行っていないため、`V-04`の一部観測は変更しません。

同日の`p0-22`では、`p0-21`の許可を使って統合支援コードを実サービス実行しました。方式Cの本人照合後、別の空の専用Chromeで人がログイン、エディタ往復、Turnstile、承認、提出ボタンを操作し、1件が受理されました。提出ID取得13 ms後に同じIDだけのV-04を開始し、`WJ`を5回、最終判定`AC`を1回、UTC時刻付きで取得したため`V-04`は合格です。サーバー指定1秒より長い最小2秒間隔を5回適用し、接続・リクエスト・ポーリング上限も分離して実動したため`V-09`も合格です。新規提出、自動再送、同一承認内の再提出はそれぞれ1件、0回、0回でした。P0で残る未合格は再設計後の`V-10`です。

同日の`p0-23`では、方式Aの検証支援コードを作成して実サービスで5回実行しました。最初の4回はKeychain入出力、子プロセス診断、`Set-Cookie`責務の境界不備を安全側で停止して修正しました。5回目はCDP、WebDriver、リモートデバッグを使わない空の可視専用Chromeで、人が公式互換性診断、login、本人アカウント、Cookie取得を操作しました。拡張が`REVEL_SESSION`だけを取得し、本人照合済みの値をKeychainへ保存した後、新規Node.jsプロセスが同じ本人を再照合できました。専用Chrome、残存対象プロセス、一時profile、一時Swift実行ファイル、loopback、Keychain項目の後始末も確認したため、`V-10`は合格です。追加提出は0件です。`V-11`のCookie更新、失効、競合、秘密情報保管庫障害は未実施のまま残します。

2026年8月13日の`p1-01`では、`p0-22`で保持した所有者専用状態を、提出処理とは別に起動したV-06専用プロセスから読みました。方式Cで本人照合後、対象ID 1件だけをGETし、`FINAL`と最終判定`AC`を再取得しました。ソースコード入力、提出処理、POST、追加提出、提出一覧走査、自動再試行は0件であり、重複提出せずに回復できたため`V-06`は合格です。P1は2/4になりました。

同日の`p1-02`では、認証情報を受け取らないV-07専用プロセスを作成し、Cookieなしの`GET /settings` 1回が302のログイン誘導となる実サービス対照と、固定入力13件のローカル制御を実行しました。サーバー由来の明示期限が過ぎた場合だけ期限切れとし、期限不明のログイン誘導を未認証または期限切れに留めたうえで、拒否、ページ構造変更、通信障害も別分類にできたため`V-07`は合格です。P1は3/4になりました。実サービスの期限切れ、Cookie更新・失効、競合、秘密情報保管庫障害は`V-11`へ残します。

同日の`p1-03`では、方式Aの実サービス観測とローカル制御を分けて`V-11`を実行しました。実サービスの3回の`GET /settings`で`REVEL_SESSION`の値を変更する`Set-Cookie`と`Max-Age`を観測し、更新候補の本人照合後にKeychainへ世代付きで置換しました。別の新規プロセスが保存値を読み、同じ本人を再照合できました。固定入力9件と一時Keychain項目では、期限非推測、明示期限切れ、失効、更新競合、読取・書込障害を提出前停止へ関連付けたため`V-11`は合格です。P1は4/4になりました。

同日の`p2-01`では、`p0-22`の`submission-A`を追加提出なしで再利用し、`V-08`を実行しました。方式Cで本人照合後、問題・言語・判定を固定した本人提出一覧の先頭1ページだけをGETし、記録済みの提出秒・問題・言語・最終判定に一致する候補を1件へ絞りました。同じ行の`FINAL` / `AC`を取得時刻付きで保持しながら、実行時間11 msを11 ms、メモリ9,172 KiBを9,392,128 byteへ正規化しました。固定入力では欠損と未知形式を値なしで保持しても判定保存を継続できたため`V-08`は合格です。`p0-22`のローカル送信元ファイルサイズとAtCoderのコード長表示は一致しなかったため、前者をリモート提出の識別条件には使いません。これによりP2は2/2となり、全11項目が合格しました。
